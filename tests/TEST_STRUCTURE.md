# Test Structure (local pointer)

The authoritative documentation for the test layout lives in:

- `docs/testing_guide.md` – short overview (recommended first read)
- `docs/tests_overview.md` – full detailed map of all tests

This file is intentionally minimal to avoid duplication. Please update the docs in `docs/` instead.

